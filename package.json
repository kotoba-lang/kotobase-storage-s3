{
  "name": "kotobase-storage-s3",
  "private": true,
  "description": "Test-only harness deps. miniflare provides a real R2Bucket binding so r2-client is exercised against an R2 API implementation rather than a mock written to match it.",
  "scripts": {
    "test": "nbb --classpath \"$(clojure -Spath -M:cljs-test)\" test/run.cljs && nbb --classpath \"$(clojure -Spath -M:cljs-test)\" test/r2.cljs && npm run test:engine",
    "test:engine": "shadow-cljs compile engine-r2 && node out/engine-r2.js"
  },
  "devDependencies": {
    "miniflare": "^4.0.0",
    "nbb": "^1.4.208",
    "shadow-cljs": "3.4.11",
    "@noble/hashes": "^1.4.0",
    "@noble/curves": "^1.7.0",
    "@ipld/dag-cbor": "^9.2.1",
    "multiformats": "^13.3.1"
  }
}
